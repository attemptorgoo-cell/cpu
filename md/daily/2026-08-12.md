# 2026-08-12

## 今日完成

- 将数据 RAM 改为同步读。
- 完成一周期 Load-use stall：保持 PC/IF/ID，ID/EX 注入 bubble，Load 正常前进。
- Load 数据在 WB 完成格式化，并复用 WB 到 EX 的旁路。
- 增加并归档 Test 11 专项测试和 Test 12 综合存储回归。
- 建立 `md/` 统括文档与按日期记录的结构。

## 验证

- Test 11：通过，精确观察到 4 次一周期 Load-use stall。
- Test 12：通过，全部寄存器/RAM 检查正确，精确观察到 10 次 stall。

## 决定

- 不逐个重跑旧测试；同步 RAM 重构后用 Test 12 覆盖相关存储、旁路、分支和错误路径行为。
- 后续对话和日志只保留验证结论，不保存完整 Vivado 输出。
- 每天可新开对话，以 `md/STATUS.md` 作为当前状态入口。

## 明日第一步

- 给 decoder 增加 `uses_rs1/uses_rs2`，消除未使用字段造成的假 Load-use stall，并增加专项测试。
