# 2026-08-16

## 今日完成

- 完成紧邻 Load→Store-data 的 WB→MEM 晚旁路。
- Load-use 判断仅豁免纯 Store.rs2 相关；Store.rs1 地址相关仍停一拍。
- 归档 Test 14–16；活动测试恢复为 Test 16，并再次完成 Load/Store 综合回归。

## 验证

- Test 14：通过；2 次地址依赖 stall，6 次 Store commit。
- Test 15：通过；非匹配非零值和匹配零值边界正确，0 stall，4 次 Store commit。
- Test 16：再次通过；Test 12 的寄存器/RAM 结果全部保持，post-forwarding stall 精确为 9 次。

## 决定

- `use_rs2` 继续表达 Store 确实读取 rs2；是否 stall 由数据首次需要的流水级决定。
- 历史 Test 12 保留 10-stall 快照，新的 9-stall 综合结果单独归档为 Test 16。
- 当前单测试 GUI 流程继续使用 Run Behavioral Simulation；Tcl 批处理留到确有批量回归需求时。

## 未解决问题

- IF 仍使用本机绝对指令路径，`cpuOutput` 尚未驱动。
- `wb_ex_bus_t` 现在同时送往 EX 和 MEM，名称范围比实际用途窄。
- 尚无外部内存握手、异常、CSR、中断和地址对齐异常。

## 下一步

- 先完成可移植路径、未驱动端口和旁路总线字段/命名的工程清理小闭环。
